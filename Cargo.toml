[package]
name = "micrograd"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = "0.13.4"
serde = "1.0.229"
tokio = "1.53.1"

[editor]
# Set the number of typed characters to trigger completion (default is 3)
completion-trigger-len = 1
